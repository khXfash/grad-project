name: bracelet_sync
description: "BraceletSync - Mental Health & Stress Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  camera: ^0.12.0+1
  cloud_firestore: ^6.2.0
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  google_fonts: ^8.0.2
  google_generative_ai: ^0.4.7
  intl: ^0.20.2
  permission_handler: ^12.0.1
  provider: ^6.1.5+1
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
